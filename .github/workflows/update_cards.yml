name: Обновление карточек из Google Таблицы

on:
  schedule:
    - cron: '0 22 * * *' # Каждый день в 3:00 по Ташкенту (22:00 UTC)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout репозиторий
        uses: actions/checkout@v3

      - name: Установка Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Установка зависимостей
        run: npm install

      - name: Запуск обновления карточек
        run: node update_cards.js

      - name: Коммит изменений
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git add index.html
          git commit -m "🔄 Автообновление карточек $(date +'%Y-%m-%d %H:%M:%S')" || echo "Нет изменений"
          git push
